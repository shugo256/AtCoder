#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
#include <string>

using namespace std;

int main() {
    int x, a;
    cin >> x >> a;
    cout << (x < a ? 0 : 10) << '\n';
    return 0;
}
