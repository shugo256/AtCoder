#include <cstdio>
#include <algorithm>

#define mod (long)(1e9+7)

//やりかけ

using namespace std;

int main() {
    int n, x;
    long sum=0;
    scanf("%d", &n, &x);
    int s[n];
    for (int i=0; i<n; i++) scanf("%d", s+i);
    sort(s, s+n);
    for (int i=0; i<n; i++) {
        
    }
    printf("%d\n", n);
    return 0;
}