#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
#include <string>
#include <atcoder/modint>

#define amax(a, b) a = std::max(a, b)
#define amin(a, b) a = std::min(a, b)

using ll = long long;
using modint = atcoder::modint1000000007;

int main() {
    int n;
    std::cin >> n;
    std::vector<char> cs(4);
    for (auto &ci : cs) std::cin >> ci;

    std::vector dp(n, std::vector<modint>(4, 0));
    if (caa == 'A')
    std::cout << n << '\n';
    return 0;
}